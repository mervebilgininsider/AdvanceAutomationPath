apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: test_metrics
    user: admin
    secureJsonData:
      password: password123
    isDefault: true
    editable: true
